<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Installation & Package Management -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Background Keep-Alive & Boot -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- PROV-BEHAVE-013 (gitea#92): not granted at install time (signature/privileged level),
         but grantable to a normal app with a single one-time
         `adb shell pm grant com.cfox.droidmesh android.permission.WRITE_SECURE_SETTINGS` that
         persists across every future app update. Once held, ProvisioningAuditor writes
         accessibility Settings.Secure keys directly instead of through the fragile in-app
         loopback ADB client, which needs a fresh on-screen auth tap for every new/lost key. -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- GTV sticks have no touchscreen; the app is driven by d-pad there,
         so a store/launcher must not filter or reclassify the install on
         this implicit feature requirement (gitea#81). -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".DroidMeshApp"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:icon="@drawable/ic_notification"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/ic_notification"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroidMesh"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- The Android TV entry. GTV home launchers (the stock one
                 and third-party ones such as Projectivy) file an app
                 under their TV row and draw its banner only when it
                 answers to this category; without it the app files as a
                 mobile app with the square icon (gitea#81). A second
                 intent-filter on the same Activity, so the LAUNCHER
                 filter above, and every phone/tablet launcher, launch
                 intent resolution, and recovery path that depends on it,
                 stays unchanged. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auto-Install Accessibility Service. Must stay exported so the
             system's AccessibilityManagerService can bind it, but the
             android:permission attribute below already restricts every
             caller, start and bind alike, to holders of
             android.permission.BIND_ACCESSIBILITY_SERVICE, a signature-level
             permission no third-party app can hold (INST-BEHAVE-012,
             gitea#39). -->
        <service
            android:name=".service.AutoInstallService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Persistent Foreground Service (HTTP Trigger Server on :2325).
             Not exported: DroidMesh always starts this itself from its own
             process; the HTTP API on :2325 is the only supported external
             trigger surface (API-BEHAVE-031, gitea#39). -->
        <service
            android:name=".service.UpdaterForegroundService"
            android:exported="false" />

        <!-- Boot Completed Receiver to auto-start foreground HTTP listener -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for APK dispatch -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.cfox.droidmesh.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
