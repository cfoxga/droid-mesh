<?xml version="1.0" encoding="utf-8"?>
<!--
    DroidMesh's entire native UI is this thin WebView shell (UI-BEHAVE-005/006). Every screen —
    Overview, Mesh, Apps, Logs, Settings — is rendered by the embedded Web UI
    (assets/web/index.html) served by LocalHttpServer; there is no separate native view tree.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ks_dark_bg">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/ks_dark_bg" />

</FrameLayout>
